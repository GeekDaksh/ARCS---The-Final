# ARCS Phase 1 — Python dependencies
# Install with:  pip install -r requirements.txt

# Core scientific computing
numpy>=1.24
pandas>=1.5
scipy>=1.10

# Physics-Informed Neural Network (PINN)
# Primary backend — strongly recommended
torch>=2.0

# PINN fallback backend (used automatically when torch is absent)
scikit-learn>=1.3

# Optional: symbolic regression (was used by genetic_programming.py, now replaced by PINN)
# Uncomment only if you want to run the old GP corrector alongside for comparison
# pysr>=0.18

flask==3.1.0
flask-cors==5.0.0